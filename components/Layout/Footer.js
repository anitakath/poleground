import styles from './Layout.module.css'

const Footer = () =>{

    return(
        <div className={`${styles.container} min-h-30 p-2`}>
            footer
        </div>
    )
}

export default Footer